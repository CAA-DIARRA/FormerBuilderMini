// app/components/FormClient.tsx
"use client";
import { useState } from "react";

export default function FormClient({ form, lang }: { form: any; lang: "fr" | "en" }) {
  // ... TON code du composant ici (tes champs, tes radios, etc.) ...
  return <div>...</div>;
}
